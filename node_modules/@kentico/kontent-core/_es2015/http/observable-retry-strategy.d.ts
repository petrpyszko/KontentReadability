import { Observable } from 'rxjs';
import { IRetryStrategyOptions } from './http.models';
export declare class ObservableRetryStrategy {
    strategy: (options: IRetryStrategyOptions, internal: {
        startTime: Date;
    }) => (errorObs: Observable<any>) => Observable<number>;
}
export declare const observableRetryStrategy: ObservableRetryStrategy;
